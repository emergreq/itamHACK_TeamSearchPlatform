# Hackathon Team Search Platform 🚀

Платформа для поиска команды на хакатон с авторизацией через Telegram бот.

## Возможности

- 🔐 **Авторизация через Telegram бот** - безопасный вход без паролей
- 👤 **Профиль пользователя** - заполнение информации о себе, навыках и роли
- 🔍 **Поиск участников** - фильтрация по ролям и статусу поиска команды
- 💬 **Встроенная система сообщений** - общение внутри платформы
- 🔔 **Telegram уведомления** - получение уведомлений о новых сообщениях в боте

## Технологии

### Backend
- Node.js + Express
- MongoDB (база данных)
- Telegram Bot API

### Frontend
- HTML5/CSS3
- JavaScript (Vanilla)

## Установка и запуск

### Требования
- Node.js (версия 14+)
- MongoDB (локально или MongoDB Atlas)
- Telegram Bot Token

### 1. Клонирование репозитория

```bash
git clone https://github.com/emergreq/itamHACK_TeamSearchPlatform.git
cd itamHACK_TeamSearchPlatform
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный **токен** и **username** бота

### 4. Настройка окружения

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Заполните файл `.env`:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/hackathon_platform

# Server Configuration
PORT=3000
SESSION_SECRET=your_random_secret_key_here

# Application URL
APP_URL=http://localhost:3000
```

### 5. Запуск MongoDB

#### Локальный MongoDB:
```bash
mongod
```

#### Или используйте MongoDB Atlas (облачный):
Создайте бесплатный кластер на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas) и используйте connection string в `.env`

### 6. Запуск приложения

#### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

#### Обычный режим:
```bash
npm start
```

Приложение будет доступно по адресу: `http://localhost:3000`

## Использование

### 1. Авторизация

1. Откройте приложение в браузере
2. Найдите вашего Telegram бота
3. Отправьте команду `/start`
4. Скопируйте полученный код авторизации
5. Вставьте код в форму на сайте или перейдите по ссылке из бота

### 2. Заполнение профиля

На странице "О себе" заполните:
- Имя
- Роль (Frontend, Backend, Designer, и т.д.)
- Навыки
- Опыт
- Информацию о себе
- Статус поиска команды

### 3. Поиск команды

На странице "Поиск команды":
- Просмотрите список участников
- Используйте фильтры по роли и статусу
- Напишите сообщение заинтересовавшему участнику

### 4. Обмен сообщениями

На странице "Сообщения":
- Просматривайте список бесед
- Читайте и отправляйте сообщения
- Получайте уведомления в Telegram о новых сообщениях

## Структура проекта

```
itamHACK_TeamSearchPlatform/
├── server/
│   ├── config/
│   │   ├── database.js          # Подключение к MongoDB
│   │   └── telegramBot.js       # Настройка Telegram бота
│   ├── models/
│   │   ├── User.js              # Модель пользователя
│   │   └── Message.js           # Модель сообщения
│   ├── routes/
│   │   ├── auth.js              # Роуты авторизации
│   │   ├── profile.js           # Роуты профиля
│   │   └── messages.js          # Роуты сообщений
│   ├── middleware/
│   │   └── auth.js              # Middleware авторизации
│   └── index.js                 # Главный файл сервера
├── public/
│   ├── css/
│   │   └── styles.css           # Стили
│   ├── js/
│   │   └── app.js               # Frontend логика
│   └── index.html               # Главная страница
├── .env.example                 # Пример конфигурации
├── .gitignore
├── package.json
└── README.md
```

## API Endpoints

### Авторизация
- `POST /api/auth/login` - Вход с кодом авторизации
- `GET /api/auth/me` - Получить текущего пользователя
- `POST /api/auth/logout` - Выход

### Профиль
- `GET /api/profile` - Получить свой профиль
- `PUT /api/profile` - Обновить профиль
- `GET /api/profile/users` - Получить список пользователей
- `GET /api/profile/:userId` - Получить профиль пользователя

### Сообщения
- `GET /api/messages/conversations` - Получить список бесед
- `GET /api/messages/:userId` - Получить сообщения с пользователем
- `POST /api/messages` - Отправить сообщение
- `GET /api/messages/unread/count` - Количество непрочитанных сообщений

## Разработка

### Запуск в режиме разработки
```bash
npm run dev
```

### Тестирование
```bash
npm test
```

## Лицензия

MIT

## Авторы

Team Search Platform - создано для хакатонов 🚀